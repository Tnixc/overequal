package dev.overequal

/**
 * Entry point placeholder. Replaced in a later checkpoint by the real CLI/bot
 * dispatcher once the data, visualization and bot layers are in place.
 */
fun main(args: Array<String>) {
    println("overequal — Discord dataviz bot (scaffold). args=${args.joinToString(" ")}")
}
